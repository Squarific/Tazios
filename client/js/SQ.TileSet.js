var SQ = SQ || {};

SQ.TileSet = function TileSet () {
	this.tiles = [];
};

SQ.TileSet.prototype.addTileSet = function (image, tileWidth, tileHeight) {
	
};

SQ.TileSet.prototype.addTile = function (number, image, x, y, width, height) {
	
};