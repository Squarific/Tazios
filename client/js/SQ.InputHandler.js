var SQ = SQ || {};

SQ.InputHandler = function InputHandler () {
	
};

SQ.InputHandler.prototype.addKeypressListeners = function keyPressed (listeners) {
	
};

SQ.InputHandler.prototype.addKeyupListeners = function keyPressed (listeners) {
	
};

SQ.InputHandler.prototype.adKeydownListeners = function keyPressed (listeners) {
	
};